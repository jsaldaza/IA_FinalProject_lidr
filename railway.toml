[build]
builder = "dockerfile"
dockerfilePath = "Dockerfile"
watchPatterns = ["Saldazia-backend/**"]

[deploy]
startCommand = "npm start"
restartPolicyType = "on-failure"
restartPolicyMaxRetries = 3

[variables]
NODE_ENV = "production"