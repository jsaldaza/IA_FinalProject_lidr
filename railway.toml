[build]
builder = "dockerfile"
dockerfilePath = "Saldazia-backend/Dockerfile"

[deploy]
startCommand = "npm start"
restartPolicyType = "on-failure"
restartPolicyMaxRetries = 3

[variables]
NODE_ENV = "production"